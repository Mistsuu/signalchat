const {
  getLocalStorage,
  setLocalStorage,
} = require("../utils/storage.util");

const {
  randomBufHex
} = require("../utils/buffer.util");

const {
  StorageConstant
} = require("../const");

const preloadInit = () => {

}

module.exports = {
  preloadInit,
}